// Function Declaration
function sum(x, y) {
  return x + y;
}

// function Expression
const sub = function () {
  return x - y;
};

// Named Function Expression
const mul = function multiply(x, y) {
  return x * y;
};
